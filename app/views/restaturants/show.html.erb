<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @restaturant.name %>
</p>

<p>
  <strong>Address:</strong>
  <%= @restaturant.address %>
</p>

<%= link_to 'Edit', edit_restaturant_path(@restaturant) %> |
<%= link_to 'Back', restaturants_path %>
